worker_processes auto;
events { worker_connections 1024; }

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  tcp_nopush    on;
  tcp_nodelay   on;
  keepalive_timeout 65;
  server_tokens off;

  gzip on;
  gzip_vary on;
  gzip_min_length 1024;
  gzip_proxied any;
  gzip_comp_level 6;
  gzip_types
    text/plain
    text/css
    text/javascript
    application/javascript
    application/json
    application/xml
    application/rss+xml
    image/svg+xml;

  server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Long cache for hashed Next build assets
    location /_next/static/ {
      access_log off;
      expires 1y;
      add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Static assets from public/: same-origin, long cache
    location ~* \.(?:png|jpg|jpeg|gif|svg|webp|avif|ico|woff2?|ttf|eot|css|js|map)$ {
      expires 30d;
      add_header Cache-Control "public, max-age=2592000";
      try_files $uri =404;
    }

    # Trailing-slash routes (Next exports as <route>/index.html)
    location / {
      try_files $uri $uri/ $uri.html $uri/index.html /index.html;
    }

    location = /404.html {
      internal;
    }
    error_page 404 /404.html;
  }
}
